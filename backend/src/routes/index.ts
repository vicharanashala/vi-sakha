// Backend global routes definition
export const API_PREFIX = 'api';
export const API_VERSION = 'v1';

export const routes = {
  auth: `${API_PREFIX}/auth`,
  users: `${API_PREFIX}/users`,
  rag: `${API_PREFIX}/rag`,
};
