{
  "name": "@visakha/reranking-service",
  "version": "1.0.0",
  "private": true,
  "description": "Pluggable reranker (cross-encoder local + Cohere API)",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": { "build": "tsc", "clean": "rimraf dist" },
  "dependencies": {
    "@visakha/shared-types": "*",
    "@visakha/shared-utils": "*",
    "@visakha/config": "*"
  },
  "devDependencies": { "typescript": "^5.1.3" }
}
