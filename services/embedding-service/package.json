{
  "name": "@visakha/embedding-service",
  "version": "1.0.0",
  "private": true,
  "description": "Embedding abstraction with retry, circuit-breaking, and batch processing",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": { "build": "tsc", "clean": "rimraf dist" },
  "dependencies": {
    "@visakha/shared-types": "*",
    "@visakha/shared-utils": "*",
    "@visakha/config": "*"
  },
  "devDependencies": { "typescript": "^5.1.3" }
}
